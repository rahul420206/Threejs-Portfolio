[build]
  command = "npm run build"
  publish = "dist"

# Proxy /api/* requests to Azure backend
[[redirects]]
  from = "/api/*"
  to = "https://rahul-portfolio-backend.azurewebsites.net/api/:splat"
  status = 200
  force = true
